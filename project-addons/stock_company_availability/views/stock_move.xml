<?xml version="1.0" encoding="utf-8"?>
<odoo>

     <record id="view_picking_move_tree" model="ir.ui.view">
            <field name="name">stock.picking.move.tree</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_picking_move_tree"/>
            <field name="arch" type="xml">
                 <field name="location_id" position="replace">
                     <field name="location_id"
                         readonly="1"
                         invisible="context.get('picking_code')=='incoming'"
                     />
                 </field>
                <field name="location_dest_id" position="replace">
                     <field name="location_dest_id"
                         readonly="1"
                         invisible="context.get('picking_code') == 'outgoing'"
                     />
                 </field>
                 <field name="quantity_done" position="attributes">
                     <attribute name="attrs"></attribute>
                 </field>
                 <field name="reserved_availability" position="after">
                     <field name="qty_available_global"
                         options='{"fg_color": "chocolate: 1==1"}'
                         invisible="context.get('picking_state') != 'partially_available'"
                     />
                     <field name="global_stock_loc_display_name"
                           options='{"fg_color": "chocolate: 1==1"}'
                           invisible="context.get('picking_state') != 'partially_available'"
                           />
                     <field name="global_stock_loc_ids"  widget="many2many_tags" invisible="1"/>
                 </field>

            </field>

     </record>
</odoo>
